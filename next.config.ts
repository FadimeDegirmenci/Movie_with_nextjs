/** @type {import('next').NextConfig} */
// const nextConfig = {}

// module.exports = nextConfig





/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['image.tmdb.org'],
  },
};

module.exports = nextConfig;



// next.config.js

module.exports = {
  images: {
    domains: ['image.tmdb.org'],
  },
};
